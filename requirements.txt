# Bisakerja Model API serving runtime dependencies
# Scope: REST API + Phase 25 TensorFlow inference only.
# Keep notebook/training-only packages (pandas, pyarrow, fastparquet, matplotlib, etc.) in training/requirements.txt.
# Runtime target: Python 3.13.x (Phase 25 recorded Python 3.13.11).

fastapi==0.123.5
uvicorn[standard]==0.47.0
pydantic==2.12.5
python-multipart==0.0.29

# Phase 25 model runtime recorded by inference_smoke_fixture.json.
# Python 3.13 requires NumPy 2.1+ through ml-dtypes; NumPy 1.26.x has no cp313 wheel.
tensorflow==2.21.0
keras==3.14.1
numpy==2.1.3

# Approved embedding runtime: artifact-declared E5 model via sentence-transformers.
# Staging image defaults to intfloat/multilingual-e5-small; intfloat/e5-base-v2 remains rollback artifact support.
# Dockerfile preinstalls CPU-only torch from the PyTorch CPU index before this line to avoid CUDA-sized runtime layers.
sentence-transformers==5.5.1
