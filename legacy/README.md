# Legacy Project Snapshot

Folder ini berisi snapshot project model lama sebelum rebuild training production-ready dimulai di root project.

Isi utama:

- `api/` — FastAPI prototype lama.
- `models/` — artifact model lama.
- `dataset/` — dataset lama.
- `artifacts/`, `cache/`, `reports/`, `logs/` — output training/inference lama.
- `bisakerja_model_training_FINAL.ipynb` — notebook training lama.

Dokumen roadmap dan referensi API dipindahkan ke root project:

- `../GAP_MODEL_TRAINING.md`
- `../TODOS.md`
- `../references/`
