# Notebook training runtime dependencies
# Use a TensorFlow-compatible Python runtime for Phase 25 model cells.
# Phase 13 requires parquet support; Phase 19.5+ requires real E5 embeddings.
pandas==3.0.3
pyarrow==24.0.0
fastparquet==2026.5.0
sentence-transformers==5.5.1
# Python 3.13 requires NumPy 2.1+ through ml-dtypes; keep training and serving aligned.
numpy==2.1.3
tensorflow==2.21.0
tensorboard==2.20.0
